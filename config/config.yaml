# Runtime config (runner/logging only)

paths:
  world_rules: "config/world_rules.yaml"
  agent_params: "config/agent_params.yaml"

malmo:
  port: 9000
  server: "127.0.0.1"
  role: 0
  experiment_id: "test_experiment"
  mission: "missions/mountain_train.xml"

logging:
  level: "INFO"
  log_dir: "logs"
